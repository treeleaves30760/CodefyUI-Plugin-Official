{
  "name": "Hello-World: the smallest possible plugin demo",
  "description": "HelloPlugin → Print. Edit the `name` param on HelloPlugin to see different greetings in the Execution Log. The minimal demonstration of how a plugin node connects to a built-in (Print).",
  "nodes": [
    { "id": "start-1", "type": "Start",       "position": { "x": -240, "y": 0 }, "data": { "params": {} } },
    { "id": "hello",   "type": "HelloPlugin", "position": { "x":   40, "y": 0 }, "data": { "params": { "name": "CodefyUI" } } },
    { "id": "print",   "type": "Print",       "position": { "x":  340, "y": 0 }, "data": { "params": { "label": "Greeting" } } }
  ],
  "edges": [
    { "id": "trig", "source": "start-1", "target": "hello", "sourceHandle": "trigger", "type": "trigger" },
    { "id": "e1",   "source": "hello",   "target": "print", "sourceHandle": "greeting", "targetHandle": "value" }
  ]
}
